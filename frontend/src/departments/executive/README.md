# Executive Department (frontend)

Composition entry for the Executive Command Seat dashboard. Today this is a
thin re-export layer over `src/components/*` so `App.tsx` continues to import
components from their original paths.

Backend counterpart: `backend/departments/executive/`.
